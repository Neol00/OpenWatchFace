# partitions_s3_16mb.csv — custom 16 MB layout for the 16 MB S3 boards:
#   - ESP32-S3-Touch-LCD-1.47
#   - ESP32-S3-Touch-AMOLED-1.8   (same 16 MB flash; no board-specific changes)
#
# Total flash on these boards = 16 MB (0x1000000); S3-1.47 chip is W25Q128JVSI
# (128 Mbit). Sizes below are flash-size-driven, not board-driven, so both share
# this table verbatim.
# NO OTA: app1 was removed (flashing is always by cable to app0), freeing its 4 MB
# slot for a 1 MB EXTENDED NVS partition ("nvsext" — the firmware's Preferences
# store, see BOARD_NVS_EXT_LABEL in the board header) with the remainder going to
# the on-flash FAT filesystem. Same scheme as the S3-2.06's 32 MB table.
# SELECT IT in the IDE the same way as the C6 table: Tools > Partition Scheme =
# "Custom" and make this the active partitions.csv for the S3-1.47 build (rename to
# partitions.csv per build), or wire a board build.partitions entry.
#
# CRITICAL — the same fixed-offset rules as the 32 MB table apply (or BLACK SCREEN):
# partition table at 0x8000, boot_app0 at 0xe000 (so otadata at 0xe000), the app
# .bin at 0x10000 (so app0 starts at exactly 0x10000). The head nvs stays 20 KB —
# it still serves the SYSTEM consumers of the default "nvs" partition (WiFi
# PHY/cal data, NimBLE bonds); the firmware's Preferences live in "nvsext".
#
# Sizing: app0 0x10000..0x410000 (4 MB, ~1.9 MB app -> ~2 MB headroom), nvsext
# ..0x510000 (1 MB), FFat fills the rest up to the coredump at the top of the
# 16 MB part: 0x510000..0xFF0000 = 0xAE0000 (~10.875 MB FAT). Nothing exceeds
# 0x1000000.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x400000,
nvsext,     data, nvs,      0x410000,  0x100000,
ffat,       data, fat,      0x510000,  0xAE0000,
coredump,   data, coredump, 0xFF0000,  0x10000,
